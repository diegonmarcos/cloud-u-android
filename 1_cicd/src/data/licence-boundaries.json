{
  "_doc": "Source-licence boundary policy. One entry per upstream whose tree is NOT uniformly under one licence, naming the directories our redistributable artefact must never reach into. Consumed by 1_cicd/src/scripts/cloud-android-licence-boundary-guard.py; the guard hardcodes no path, no upstream and no symbol. Add an upstream = one entry here, no script change.",
  "_doc_why": "AFFiNE was scoped on 2026-09-09 as 'MIT except packages/backend', and de-clouding was authorised on that basis. It was wrong by one directory: packages/common/native is EE too, and the Android Rust crate statically links it into the APK through Cargo, where no JavaScript-level audit can see it. A boundary described in prose gets read once and misremembered; a boundary described in data gets checked every build.",
  "upstreams": {
    "affine": {
      "repo": "https://github.com/toeverything/AFFiNE.git",
      "pinned_revision": "cfda4858d550c46ec1cd0b574801d7becdd6625b",
      "_doc_pinned_revision": "Full sha, never the branch name. 'canary' is AFFiNE's default branch and moves; this fleet has already been bitten by a rolling-tag hash race. Bump this deliberately, re-run the guard, re-run the patch series.",
      "permissive_licence": "MIT",
      "restricted_licence": "AFFiNE Enterprise Edition",
      "restricted_dirs": [
        "packages/backend",
        "packages/common/native"
      ],
      "_doc_restricted_dirs": "Verbatim from the root LICENSE: content under these two directories is EE-licensed, everything else is MIT. The EE text forbids copying, distributing and sublicensing — which is what shipping an APK, and what vendoring source into this repo, both are. packages/common/native has NO package.json, so it is invisible to a yarn-workspace audit; it is reachable only through Cargo.",
      "scan_roots": [
        "packages/frontend/apps/android",
        "packages/frontend/mobile-native"
      ],
      "_doc_scan_roots": "The Android artefact's own source. Everything compiled into the APK originates under one of these two; the rest of the monorepo is desktop/server/web and is not our redistributable.",
      "markers": [
        "backend/server",
        "backend/native",
        "common/native",
        "affine_common",
        "@affine/server"
      ],
      "_doc_markers": "Literal substrings that betray a reach into a restricted dir across ALL THREE of this app's build systems: a Gradle/Apollo schema path, a Cargo path-dependency crate name, a yarn workspace package name. Two traps are baked in here. (1) Cargo RENAMES the directory to a crate name, so scanning for the directory path alone finds nothing — which is how the 2026-09-09 scoping missed affine_common. (2) The Gradle reach is written as a RELATIVE path — schemaFiles.from(\"../../../../../backend/server/src/schema.gql\") — so a marker of 'packages/backend' matches nothing. Markers are therefore the directory tails, not the repo-root paths. Both traps were found by running the guard, not by reading it: the first draft used repo-root paths and silently reported only 2 of the 3 reaches.",
      "scan_extensions": [
        ".gradle",
        ".kts",
        ".toml",
        ".rs",
        ".kt",
        ".java",
        ".ts",
        ".tsx",
        ".js",
        ".json"
      ],
      "known_reaches": {
        "_doc": "The reaches present in PRISTINE upstream at pinned_revision. The guard reports found-vs-known so a NEW reach introduced by an upstream bump is loud, and so the de-cloud work has an explicit checklist. A de-clouded tree must show ZERO; anything here is a to-do, not an allowance.",
        "packages/frontend/apps/android/App/service/build.gradle": "Apollo codegen reads backend/server/src/schema.gql via a relative path — removed by dropping the :service gradle module",
        "packages/frontend/mobile-native/Cargo.toml": "affine_common path-dependency on packages/common/native — NOT removed by de-clouding; needs the Rust crate patched",
        "packages/frontend/mobile-native/src/lib.rs": "use affine_common::hashcash::Stamp for the cloud-auth proof-of-work — NOT removed by de-clouding"
      }
    }
  }
}
