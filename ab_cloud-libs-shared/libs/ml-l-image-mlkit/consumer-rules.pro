# No extra rules needed; ML Kit's own keep rules ship inside the artifact,
# exactly as libs:ml-l-text-mlkit's consumer-rules.pro documents. ZXing is a
# plain Java library with no reflection entry points of its own.