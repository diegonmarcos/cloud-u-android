<?xml version="1.0" encoding="utf-8"?>
<!-- libs:ml-l-text-mlkit — ML Kit engine implementation (#405: an ML lib
     names its own weight class and application, ml-{t|l}-{domain}-{name}).
     cloud-keyboard-libs includes this; cloud-keyboard does NOT (it binds the
     AIDL service instead). aa_cloud-superapp does not include it either — its
     build.json::modules declares no keyboard module at all. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android" />
