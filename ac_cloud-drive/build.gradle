// Root build.gradle — plugin versions only.
plugins {
    id 'com.android.application' version '8.7.3' apply false
    id 'com.android.library'     version '8.7.3' apply false
    id 'org.jetbrains.kotlin.android'            version '2.3.20' apply false
    id 'org.jetbrains.kotlin.plugin.serialization' version '2.3.20' apply false
    // The four engine libraries (#567) and the host screen that shows them are
    // Compose (#565); a @Composable source set needs the compose compiler, resolved
    // from here the way libs:bottomnav resolves it from cloud-mail's root.
    id 'org.jetbrains.kotlin.plugin.compose'       version '2.3.20' apply false
}
