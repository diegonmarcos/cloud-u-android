{
  "vendored_engine": "cloud-nix-on-droid-fork-engine.sh",
  "_doc_vendored_engine": "CI does NOT run the engine source in 1_cicd/src/scripts \u2014 it runs the VENDORED COPY at ./build.sh, from this directory. This key DECLARES that copy relationship so the workflow generator can enforce byte-identity (cloud-android-ship-repo-workflow-engine.sh, step 'verify engine \u2194 vendored build.sh parity') and go red on drift. Editing the engine alone used to change nothing at ship time, silently \u2014 which is how the ABI gate and the GHCR auto-delete gate came to exist only in the file nobody executes. Re-sync with: cp 1_cicd/src/scripts/<engine> ./build.sh",
  "forks": {
    "nixdroid": {
      "app_id": "cld.termux.nix",
      "label": "Cloud Terminal (Nix)",
      "image": "cloud-nixdroid",
      "upstream_repo": "https://github.com/nix-community/nix-on-droid-app.git",
      "tracker_dir": "ac_cloud-nix-on-droid",
      "tracker_dir_note": "THIS IS OUR CODE. The gradle project is ac_cloud-nix-on-droid/ itself, flattened at the app root like every other app here. upstream_repo/pinned_tag are provenance: they record where the tree came from, not something the build fetches. GPL provenance lives in README.md, ATTRIBUTION.md and LICENSE.",
      "pinned_tag": "e87b609",
      "license": "GPL-3.0",
      "primary_lang": "Java",
      "runtime": "native",
      "priority": 2,
      "blocked_on": null,
      "app_id_note": "RENAMED 2026-08-30, overriding the prior 'keep upstream id' decision: com.termux.nix -> cld.termux.nix, so this fork installs ALONGSIDE the official Nix-on-Droid app instead of colliding with it (same id + different signature previously forced the upstream app to be uninstalled first). The new id is EXACTLY the same byte length (14 chars) as the old one \u2014 mandatory, not cosmetic: this is termux-app-derived and, like Termux itself, would compile the applicationId into every bootstrap ELF binary as a fixed-length string were its native bootstrap module enabled (it is currently disabled \u2014 'NIX-ON-DROID: Unused' \u2014 so no bootstrap-zip patching was needed here; its Nix store instead comes from proot bind-mounts and Context.getFilesDir() at runtime, both of which follow applicationId automatically). Equal length keeps the option open without relinking anything if that module is ever re-enabled, and matches the sibling cld.termux rename in ac_cloud-termux/build.json for the same reason. See TermuxConstants.TERMUX_PACKAGE_NAME / app/build.gradle applicationId+manifestPlaceholders for the source-side rename.",
      "build": {
        "_doc": "Stock termux-app gradle: assembleRelease, signed with the ONE shared constellation keystore. signing=vault_jks_env, NOT 'keystore' \u2014 that is not a mode the engine has, and an unrecognised value matches no branch and signs with the debug key instead, silently. A debug-signed build never receives the signature-level CLOUD_EXEC permission, so the bind fails the same way it does when the app is not installed at all.",
        "gradle_task": "assembleRelease",
        "apk_glob": "app/build/outputs/apk/release/*.apk",
        "signing": "vault_jks_env",
        "keystore_dest": "app/release.jks",
        "signing_env": {
          "_doc": "Consumed by _resolve_signing in the fork engine and read back by the release signingConfig added to app/build.gradle. The tokens are the engine's vocabulary \u2014 keystore_path|store_password|key_password|key_alias \u2014 and it fails loud on anything else.",
          "TERMUX_KEYSTORE_PATH": "keystore_path",
          "TERMUX_KEYSTORE_PASSWORD": "store_password",
          "TERMUX_KEY_PASSWORD": "key_password",
          "TERMUX_KEY_ALIAS": "key_alias"
        }
      },
      "comment": "Cloud Nix-on-Droid fork."
    }
  },
  "signing": {
    "vault_keystore": "A0_keys/providers/android/release.jks",
    "vault_secrets": "A0_keys/providers/android/signing.secrets.yaml"
  },
  "release": {
    "ghcr": {
      "_doc": "DISABLED, and disabled is the TRUTH rather than a preference: ship-cloud-nix-on-droid.yml has no GHCR step at all \u2014 no docker/login, no oras push \u2014 so nothing has ever created the cloud-nixdroid package. Verified 2026-09-10 by enumerating all 431 container packages under the diegonmarcos account: cloud-nixdroid is not among them, and /user/packages/container/cloud-nixdroid answers 404 to a token that reads cloud-terminal fine. This key said `true` while that was so, which is not a harmless overstatement: regen.sh emitted the image name into the fleet manifest, the updater probed it whenever the release channel hiccuped, and GHCR answers an anonymous caller 403 DENIED \u2014 not 404 \u2014 for a package that does not exist, because confirming absence would leak private package names. That 403 is what the owner saw on the phone. gh_release below is genuinely the only channel for this app, exactly as its own _doc says. To give it a registry channel, add the push step to the workflow and flip this back \u2014 in that order, so the declaration never leads the pipeline again.",
      "enabled": false,
      "registry": "ghcr.io",
      "namespace": "diegonmarcos",
      "image": "cloud-nixdroid",
      "media_type": "application/vnd.android.package-archive"
    },
    "gh_release": {
      "_doc": "Rolling 'latest' release: the ONLY channel for this app, since GHCR would publish it private. asset_name was inherited as cloud-comms-dialer.apk when this block was copied from the dialer \u2014 an install URL pointing at a different app entirely. regen.sh's fork branch derives the store asset as image + '.apk' and so was never wrong, which is exactly why the bad value could have sat here unnoticed.",
      "enabled": true,
      "rolling_tag": "latest",
      "asset_name": "cloud-nixdroid.apk",
      "draft": false,
      "prerelease": false,
      "generate_release_notes": false,
      "tag_pattern": "ac_cloud-nix-on-droid-v*"
    },
    "variants": [
      {
        "_doc": "Same shape as ac_cloud-browser and every other per-ABI app here. CLOUDNAV_VARIANT selects one; the engine exports .abis as CLOUDNAV_ABIS for gradle and names the artifact from .gh_asset, so the two matrix jobs cannot overwrite each other in one dist dir.",
        "id": "arm64",
        "abis": [
          "arm64-v8a"
        ],
        "supported_abis": [
          "arm64-v8a",
          "armeabi-v7a"
        ],
        "artifact_debug": "cloud-nixdroid.apk",
        "gh_asset": "cloud-nixdroid.apk",
        "ghcr_tag_suffix": "",
        "update_tag": "latest"
      },
      {
        "id": "x86_64",
        "abis": [
          "x86_64"
        ],
        "supported_abis": [
          "x86_64",
          "x86"
        ],
        "artifact_debug": "cloud-nixdroid-x86_64.apk",
        "gh_asset": "cloud-nixdroid-x86_64.apk",
        "ghcr_tag_suffix": "-x86_64",
        "update_tag": "latest-x86_64"
      }
    ]
  }
}
