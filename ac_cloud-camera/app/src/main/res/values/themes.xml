<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base application theme. -->
    <style name="Theme.Camera" parent="Theme.Material3.Dark.NoActionBar">
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>

        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.App" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>

        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
    </style>

    <style name="Theme.SettingsDialog" parent="ThemeOverlay.Material3.MaterialAlertDialog.Centered">
        <item name="android:windowMinWidthMinor">100%</item>
        <item name="android:windowMinWidthMajor">100%</item>
    </style>

    <style name="ShapeAppearance.Image.PILL" parent="">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>

    <style name="Theme.Viewer" parent="Theme.Material3.DayNight">
        <item name="android:windowActionModeOverlay">true</item>
        <item name="android:windowActionBarOverlay">true</item>
        <item name="android:windowActionBar">true</item>

        <item name="windowActionModeOverlay">true</item>
        <item name="windowActionBarOverlay">true</item>
        <item name="windowActionBar">true</item>

        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>

        <item name="android:homeAsUpIndicator">@drawable/back_white</item>

        <item name="actionOverflowButtonStyle">@style/WhiteOverflowButtonStyle</item>
    </style>

    <style name="WhiteOverflowButtonStyle" parent="Widget.AppCompat.ActionButton.Overflow">
        <item name="android:tint">@android:color/white</item>
    </style>

    <!--
        The two Media Center buttons under the gallery thumbnail. One style, not two
        copies of the same eight attributes: the requirement is that a user cannot tell
        the video button from the photo button except by its icon, and duplicated
        attributes drift the first time one of them is touched alone.

        Appearance only. The layout_* attributes stay inline on both buttons: a
        LinearLayout child with no layout_width throws InflateException at startup, and
        that is not a failure mode worth putting behind a style-resolution rule to save
        two lines.

        Tinted white explicitly - the glyphs fill with ?attr/colorControlNormal, which
        Theme.Camera (Material3.Dark) resolves light today, but they must not go
        dark-on-black if that theme parent ever changes.
    -->
    <style name="Widget.Camera.MediaCenterButton" parent="">
        <item name="android:padding">12dp</item>
        <item name="android:scaleType">fitCenter</item>
        <item name="android:background">?actionBarItemBackground</item>
        <item name="android:tint">@android:color/white</item>
    </style>
</resources>
