{
  "id": "ac_cloud-office",
  "name": "cloud-office",
  "vendored_engine": "cloud-office-source-engine.sh",
  "_doc_vendored_engine": "CI runs ./build.sh, the vendored copy of 1_cicd/src/scripts/cloud-office-source-engine.sh; the workflow generator fails on drift between the two. Re-sync with: cp 1_cicd/src/scripts/cloud-office-source-engine.sh ac_cloud-office/build.sh",
  "_doc": "SOURCE BUILD, as of this commit. This directory used to be a MIRROR: ship-cloud-sheets.yml downloaded Collabora's official APK, checked it against a pinned sha256 and republished those exact bytes. That gave a strong supply-chain guarantee and zero ownership — we could not rename the app, could not move its menus, and above all could not make Text Enhance work, because the feature needs code inside the web layer and the Android shell. The owner chose ownership. So we now build from source, and the sha256-of-upstream's-bytes guarantee is replaced by a pinned-revision guarantee; see supply_chain below, which states plainly what is weaker.",
  "_doc_no_source_in_git": "THE SOURCE IS NOT IN THIS REPOSITORY AND MUST NOT BE. LibreOffice core with external/ present is multi-gigabyte; this repo has a hard rule that no file may approach 100 MB, has already had to repair a 90 MB repository, and shards its transcripts to stay under GitHub's blob limit. Committing the tree would wreck it. Instead: two pinned upstreams fetched at build time, and every edit we own as a numbered patch in patches/. The fleet's other forks (dialer, matrix, vault, media-center) vendor their patched source instead — they moved to that on 2026-08-19 — and we deliberately cannot follow them, for size and nothing else. patches/README.md justifies the divergence in full.",
  "upstream": {
    "online": {
      "_doc": "Collabora Online — the web app in the WebView (browser/) AND the Android shell around it (android/). This is the ONLY tree we patch.",
      "vendor": "Collabora Productivity",
      "url": "https://gerrit.collaboraoffice.com/online",
      "_doc_url": "GERRIT, NOT GITHUB, AND THAT IS NOT A PREFERENCE. The GitHub mirror (CollaboraOnline/online) carries the Android tree only on distro/collabora/co-25.04-mobile; the 26.04 mobile line — the one matching the 26.04.3.1 APK this app shipped while it was a mirror — exists only on Collabora's own Gerrit. Pinning the GitHub mirror would have quietly moved the owner back a whole release line. github_mirror stays recorded as a read-only convenience for code search; it is never the fetch source.",
      "github_mirror": "https://github.com/CollaboraOnline/online",
      "ref": "distro/collabora/co-26.04-mobile",
      "revision": "794af0088b0369878461cc53de45129eaf08824a",
      "_doc_revision": "A COMMIT SHA, NOT A BRANCH. VERIFIED: patches/0001 applies cleanly to this exact revision with `git am --keep-non-patch` from a clean checkout, watched, not assumed. The SAME patch generated against 25.04-mobile FAILED here on three files — 'error: browser/src/map/Clipboard.js: patch does not apply' — which is precisely the silent drift a floating ref would have caused and the reason the sha is written down. tests/patches-apply.sh refuses anything that is not a full 40-character sha.",
      "patch_mode": "apply",
      "_doc_patch_mode": "apply = the series in patches/ is applied with `git am` at materialize time against `revision`. The fleet's other forks are `vendored` (patched source committed, patches kept only as history). Reading this field is how an engine tells the two apart without knowing which app it is looking at.",
      "license": "MPL-2.0",
      "configure": {
        "_doc": "THE REBRAND IS THESE FLAGS, NOT A PATCH. android/app/appSettings.gradle is generated from android/app/appSettings.gradle.in, whose @APP_NAME@ / @APP_PACKAGE_NAME@ / @VENDOR@ / @INFO_URL@ placeholders configure.ac substitutes (AC_CONFIG_FILES, configure.ac:2923), so upstream already exposes every branding value as a knob. Patching the generated file would hardcode into the tree what a flag sets, and the patch would then conflict on every upstream touch of that file.",
        "_doc_flag_names": "EVERY NAME HERE IS VERIFIED AGAINST configure.ac AT THE PIN, BECAUSE AUTOCONF DOES NOT CHECK THEM FOR US. This block said \"--with-android-package-name\" from 05b0c6c15 until 2026-09-11. No such option exists at this revision — upstream's is --with-app-package-name (AC_ARG_WITH at configure.ac:292), and there is no @ANDROID_PACKAGE_NAME@ placeholder anywhere; the applicationId comes from @APP_PACKAGE_NAME@. An unknown --with-* does NOT fail configure: autoconf prints 'unrecognized options' to stderr and continues, so $with_app_package_name would have stayed empty and APP_PACKAGE_NAME would have taken its default. VERIFIED BY READING configure.ac:1155-1169: the default is org.collabora.app for an Android build, and com.collabora.office.Mobile only when enable_iosapp=yes. So the megabuild would have spent its money producing an APK whose applicationId is UPSTREAM'S, while android.application_id, the fleet manifest, the GHCR image and the launcher's installed-app probe all name com.diegonmarcos.cloudoffice — an app that would not exist. tests/test-configure-flags-exist.sh now derives the required flag from upstream on every run (placeholder -> configure.ac variable -> flag) and fails closed, so this cannot regress silently and a future upstream rename reports its new name instead of passing.",
        "--with-app-name": "cloud-office",
        "--with-app-package-name": "com.diegonmarcos.cloudoffice",
        "--with-vendor": "diegonmarcos.com",
        "--with-info-url": "https://linktree.diegonmarcos.com"
      },
      "gerrit_url": "https://gerrit.collaboraoffice.com/online",
      "_doc_fetch_cost": "MEASURED, so nobody plans CI around a guess: Gerrit refuses blob filtering (`warning: filtering not recognized by server, ignoring`) and will not serve an arbitrary sha (`upload-pack: not our ref`). A `git fetch --depth 1 origin distro/collabora/co-26.04-mobile` from this container produced a 471 MB .git and took roughly ten minutes. Any CI step fetching this needs that budget and a cache; it is not a thirty-second checkout."
    },
    "engine": {
      "_doc": "THE ENGINE IS A PINNED PREBUILT BINARY, NOT A BUILD. This key replaced upstream.core on 2026-09-11. WHAT CHANGED AND WHY: upstream.core pinned the separate LibreOffice `core` repo at c1013fad, read from a ref tip on 2026-09-10 and dated 2026-04-01 — five months stale against the 2026-09-03 shell it was supposed to match — and it existed so that #240 could pay for an NDK megabuild of LibreOffice. Nothing the owner asked for needs that build. patches/0001 touches nine files: five TypeScript/JavaScript in browser/, three Java/XML/JSON in android/, and settings.gradle. Not one line of C++. So both native libraries can be taken from the artefact we already pin and already trust, and the engine's own git ancestry stops being a build input at all — which is why there is no `revision` of a core repository here to keep in sync, and why Gerrit's refusal to serve arbitrary shas no longer matters to this app.",
      "kind": "prebuilt-binary",
      "_doc_two_native_halves": "READ FROM THE ARTEFACT, NOT ASSUMED. The APK carries FIFTEEN .so files, and #241's \"one monolithic 199 MB library\" is only the first of them. lib/arm64-v8a/liblo-native-code.so (199,472,488 bytes) is LAYER 1, the LibreOffice engine, the thing a megabuild would produce. lib/arm64-v8a/libandroidapp.so (6,322,632 bytes) is online's OWN C++ — wsd/, kit/, net/, common/ — and is the ONLY thing that needs the eight static archives and four header trees an APK cannot carry. The other thirteen are NSS and libc++. Because we change no C++, neither is rebuilt, and the link-time half stops being required.",
      "source": {
        "_doc": "The bytes are the ones this app mirrored until 05b0c6c15, and they are the same bytes Collabora's F-Droid index publishes — verified 2026-09-10 by streaming all 267,216,449 through sha256sum, and re-downloaded and re-inspected in full on 2026-09-11. The sha256 is the whole security argument: extracting a pinned artefact at build time is exactly the guarantee the mirror already relied on, so the engine half of this app KEEPS the supply-chain property that supply_chain._doc_weaker says the source build gives up. Only the layers we actually edit lose it.",
        "url": "https://www.collaboraoffice.com/downloads/fdroid/repo/collabora-office-mobile-26.04.3.1-155-release-arm64-v8a-2026-09-03.apk",
        "sha256": "b7ab381de96f429c2e762d0e54ee0fac7db03ad2fecfede9f4c558c767d18513",
        "size": 267216449,
        "abi": "arm64-v8a",
        "_doc_not_committed": "199 MB CANNOT BE COMMITTED — twice GitHub's 100 MB blob cap, and this repository has already had to repair a 90 MB history. The workflow downloads this URL, checks the sha256, and unzips only the paths named below. Nothing binary enters git."
      },
      "native_libs": {
        "_doc": "Lifted verbatim out of the pinned APK and dropped into online's OWN jniLibs source directory, which is where upstream's build puts them too: android/lib/build.gradle sets jniLibs.srcDirs = ['src/main/cpp/lib'], and CMakeLists.txt.in fills that directory with `cmake -E copy`, never with a compiler. A .so in a jniLibs source dir is PACKAGED, not built. Asserted on every run by tests/test-no-engine-build-required.sh against upstream at upstream.online.revision.",
        "from_apk_dir": "lib/arm64-v8a",
        "packaged_from": "the unpacked payload: patches/0002 sets jniLibs.srcDirs to <payload>/lib, and ./build.sh build asserts every packaged .so is byte-identical to the released APK",
        "expect_count": 15,
        "_doc_expect_count": "All fifteen, by directory rather than by name: a hand-written list of .so files is a second copy of upstream's DT_NEEDED graph and goes stale the first time Collabora adds one. The count is the assertion — if the APK stops carrying exactly fifteen, the build stops rather than shipping an app that dlopens something that is not there.",
        "_doc_verified_by_extraction": "RE-VERIFIED 2026-09-11 BY EXTRACTING, not by reading the table. The ZIP central directory (6,375 entries, 591,403 bytes) was range-fetched and parsed: lib/arm64-v8a holds exactly 15 .so, expect_count agrees; 0 entries end .a, .h or .hxx, so the link-time half is confirmed absent; assets/dist holds 3,384 entries, which is the COOL web UI we rebuild. lib/arm64-v8a/libandroidapp.so and liblo-native-code.so are both STORED (compression method 0), so they can be lifted with a byte range and no inflate. libandroidapp.so was pulled out in full — 6,322,632 bytes, matching this table — and parsed as a 64-bit AArch64 ELF (e_machine 183). THE ENGINE CAN BE EXTRACTED; that question is closed and needs no further re-litigation."
      },
      "assets": {
        "_doc": "The engine's DATA half — .rdb type libraries, fonts, share/registry, config — is likewise already built, and already in the APK. Upstream's copyUnpackAssets / copyAssets / createFullConfig gradle tasks read it out of ${liboInstdir}, i.e. out of an engine build tree; taking it from the artefact is the same files by a shorter road. assets/dist is DELIBERATELY EXCLUDED: that is layer 2, the COOL web UI, and it is the thing we rebuild.",
        "from_apk_dir": "assets",
        "_doc_from_apk_dir": "ALL of assets/ except `exclude`, not a list of subdirectories: upstream's copyAssets also places license.html, notice.txt and example.odt at the assets root, and a subdirectory list silently dropped them.",
        "exclude": [
          "assets/dist"
        ],
        "to_tree_dir": "android/lib/src/main/assets",
        "rebrand": {
          "_doc": "ONE FILE IN THE PREBUILT PAYLOAD CANNOT BE TAKEN VERBATIM, AND TAKING IT VERBATIM FAILS GREEN. The prebuilt plan lifts assets/** straight out of Collabora's APK, which is right for 545 of the 546 text-config entries under assets/ (assets/dist excluded). It is WRONG for fonts.conf: upstream does not ship that file, it GENERATES it, and the generator is copyUnpackAssets itself — `filter { line.replaceAll('@@APPLICATION_ID@@', liboApplicationId) }`, the very task that _doc_BLOCKER_configure_gate (3) says prebuilt mode must disable. Disable the task and take its output from the pinned APK and you inherit COLLABORA'S substitution: <cachedir>/data/data/com.collabora.libreoffice/fontconfig</cachedir>. On Android that is another application's private data directory, unreadable and unwritable to us at a different uid, so fontconfig cannot write its cache and every document open re-scans the font set. The APK builds, signs, installs and launches; only the thing it is for degrades. MEASURED 2026-09-11, NOT ASSUMED: all 546 text-config entries under assets/ were range-read out of the pinned APK and grepped for 'com.collabora'. Exactly one matched, and it is this one. That is why `files` is a list of one rather than a guess — and why it is a LIST: the day upstream bakes the id into a second file, the tester below names it instead of passing.",
          "upstream_application_id": "com.collabora.libreoffice",
          "files": [
            "assets/unpack/etc/fonts/fonts.conf"
          ],
          "asserted_by": "tests/test-prebuilt-assets-need-rebranding.sh"
        }
      },
      "engine_revision": {
        "_doc": "PROVENANCE, NOT A BUILD INPUT — and read out of the artefact rather than out of a ref tip. assets/program/versionrc in the pinned APK says buildid=20a46c332c38; that is the twelve-character form configure.ac:927 derives for CORE_VERSION_HASH, which means the prebuilt mode can read the hash from a file the APK actually contains instead of from instdir/program/setuprc, which it does not (tests/engine-artefacts.json records setuprc as in_apk=false). Nothing fetches this revision and nothing needs it to be an ancestor of anything.",
        "buildid": "20a46c332c38",
        "read_from": "assets/program/versionrc"
      },
      "license": "MPL-2.0",
      "_doc_license": "SHIPPED UNMODIFIED AND DYNAMICALLY LINKED, WHICH IS THE EASY CASE. LibreOffice core is MPL-2.0; MPL is FILE-level copyleft, so an unmodified file carries no obligation beyond keeping the notice, and the APK already carries assets/license.html and assets/notice.txt (both checked as present by tests/engine-artefacts.json). We modify no engine file, so there is no modified engine source to publish; the files we DO modify are online's, are also MPL-2.0, and are already public as patches/0001 in a public repository. THE REAL CONSTRAINT IS NOT COPYLEFT, IT IS TRADEMARK: assets/dist/images carries collabora-office-white.svg, full-logo.svg and toolbar-bg-logo*.svg, and republishing those inside an app called Cloud Office is a Collabora trade-mark use, not a licence question. Upstream's own answer is the branding knobs we already set — --with-app-name, --with-vendor and APP_BRANDING_DIR — plus dropping those image assets. See a0_docs/eng-specs/cloud-office-prebuilt-engine-reuse.md; the fleet's licence-boundary guard models source copyleft only and has no way to express this yet.",
      "_doc_BLOCKER_configure_gate": "THE MECHANICAL THINGS STILL IN THE WAY. NONE IS A COMPILE, AND THERE ARE THREE, NOT ONE. This key said 'THE ONE MECHANICAL THING' until 2026-09-11 and named only the first; the other two were found by reading android/lib/build.gradle and android/lib/libSettings.gradle.in at the pin, and BOTH of them fail GREEN, which is worse than the one that fails loudly. (1) CONFIGURE GATE, and it is real: configure.ac:884 refuses --enable-androidapp without --with-lo-builddir, then probes that directory for instdir/program/setuprc (921), workdir/LinkTarget/StaticLibrary/liblibpng.a (930), Poco/Poco.h (1009-1012) and zstd.h/libzstd.a (1051-1058). tests/engine-artefacts.json records 0 of 6,375 APK entries ending .a/.h/.hxx, re-counted 2026-09-11, so the gate cannot be satisfied from the artefact. (2) GRADLE STILL RUNS THE NDK, AND TAKING libandroidapp.so PREBUILT DOES NOT STOP IT. android/lib/build.gradle declares externalNativeBuild in BOTH defaultConfig (lines 32-36) and the android block (74-79), pointing at src/main/cpp/CMakeLists.txt, whose add_library(androidapp SHARED ...) compiles online's own wsd/kit/net/common and links the eight static archives. So Gradle asks for the link-time half on every :lib build no matter what is already sitting in jniLibs, and #240 comes back by the side door. patches/0002 must remove that wiring, not just add a configure option. (3) THE ASSET TASKS READ AN ENGINE BUILD TREE AND FAIL SILENTLY. libSettings.gradle.in sets liboSrcRoot='@LOBUILDDIR@' and liboInstdir='@LOBUILDDIR@/instdir'; copyUnpackAssets, copyAssets and createFullConfig copy from ${liboInstdir}/program, /share and /share/fonts/truetype, and preBuild depends on them. A Gradle Copy task whose `from` does not exist SUCCEEDS AND COPIES NOTHING. In prebuilt mode that ships an APK with the engine's .so files present and its data half — .rdb type libraries, registry, fonts, config — ABSENT, after a fully green build. patches/0002 must disable these tasks explicitly in prebuilt mode so the behaviour is declared; relying on a missing directory to no-op is the same class of defect as the four testers that passed because ripgrep was absent. (4) AND FIXING (3) THE OBVIOUS WAY INTRODUCES A FOURTH, MEASURED 2026-09-11. The obvious fix for (3) is to disable the three tasks and lift assets/** out of the pinned APK instead, which is what assets.from_apk_dirs above describes. That is right for 545 of the 546 text-config entries under assets/ and WRONG for one: copyUnpackAssets does not COPY android/lib/fonts.conf, it GENERATES it, filtering @@APPLICATION_ID@@ into liboApplicationId. Collabora's APK therefore carries THEIR substitution — <cachedir>/data/data/com.collabora.libreoffice/fontconfig</cachedir> — which under our package id is another application's private data directory at a different uid, so fontconfig can neither read nor write its cache and every document open re-scans the font set. Signed, published, installed, launched, and degraded. See assets.rebrand, whose file list was derived by range-reading all 546 entries rather than guessed, and tests/test-prebuilt-assets-need-rebranding.sh, which fails closed. ALL FOUR ARE ADDRESSED BY patches/0002 (2026-09-15): configure --with-prebuilt-engine skips only the build-tree probes and reads CORE_VERSION_HASH from assets/program/versionrc; both externalNativeBuild declarations are gated off and jniLibs point at the unpacked payload; copyUnpackAssets, copyAssets and createFullConfig are DISABLED explicitly and replaced by copyPrebuiltEngineAssets; and fonts.conf is excluded from that copy and still generated for our application id by generatePrebuiltFontsConf.",
      "_doc_jni_abi_is_verified": "THE RISK #277 DID NOT WEIGH, NOW MEASURED AND CURRENTLY SAFE. Taking the native half prebuilt means OUR Java, compiled from upstream.online.revision, binds at runtime to COLLABORA'S libandroidapp.so, built for the 26.04.3.1-155 release. Those are different commits: the pin's own commit subject is 'fix(iOS): correct blank slides when presenting' (authored 2026-09-03, committed 2026-09-04 by Gerrit change 11099), an ordinary branch-tip commit and not that release tag. JNI binds BY SYMBOL NAME at first call, never at build time, so a native method added, removed or renamed between the two produces a signed, published, installable APK that dies with UnsatisfiedLinkError when the document view opens. MEASURED 2026-09-11 by extracting the .so from the pinned APK over byte ranges and reading its .dynsym: it exports exactly 8 Java_* symbols, all in org.libreoffice.androidlib (LOActivity 7, COWebViewClient 1), none of them a long overload name; the Java at the pin declares exactly those 8 and no others. They agree today. org.libreoffice.androidlib is the LIBRARY module's package and the rebrand does not touch it — --with-app-package-name sets applicationId only — so renaming the app to com.diegonmarcos.cloudoffice cannot break these symbols. tests/test-jni-abi-matches-prebuilt.sh re-checks the whole chain on every run and fails closed, so the day the pin moves away from the published bytes it reds instead of shipping a dead app."
    }
  },
  "prebuilt_mode": {
    "_doc": "THE COMPLETE LIST OF WHAT PREBUILT MODE MUST NEUTRALISE, MEASURED AT THE PIN ON 2026-09-11 BY CLONING IT. _doc_BLOCKER_configure_gate named three blockers and was still short: the release variant also shells out to `make -C ../../browser`, which is the npm/webpack build of the COOL web bundle we take from the pinned APK. That is FIVE anchors, not three, and the count mattering is the point — patches/0002 that hits four of five produces a build that either dies asking for Node or ships assets/dist twice. Every anchor below was read out of the tree at upstream.online.revision, not inferred, and tests/test-prebuilt-mode-anchors-exist.sh re-reads each one from Gerrit on every run so a moved anchor reds instead of producing a patch that applies to nothing. LINE NUMBERS ARE RECORDED FOR ORIENTATION AND ARE NOT THE ASSERTION; the anchor TEXT is.",
    "_doc_written": "patches/0002 IS WRITTEN (2026-09-15) and neutralises the first five anchors. The sixth, cool-bundle-release, it deliberately KEEPS: patches/0001 edits browser/ (the Text Enhance menu), so assets/dist must be rebuilt from this tree rather than taken from the APK — which is also why engine.assets.exclude drops assets/dist.",
    "neutralise": [
      {
        "id": "ndk-defaultConfig",
        "file": "android/lib/build.gradle",
        "line_hint": 32,
        "anchor": "externalNativeBuild {",
        "why": "One of TWO externalNativeBuild declarations. Taking libandroidapp.so prebuilt does not stop Gradle asking CMake to build it; this is #240 walking back in through the side door.",
        "action": "neutralise",
        "neutralised_by": "if (!liboPrebuiltEngine) {"
      },
      {
        "id": "ndk-android-block",
        "file": "android/lib/build.gradle",
        "line_hint": 74,
        "anchor": "path \"src/main/cpp/CMakeLists.txt\"",
        "why": "The second declaration, in the android block rather than defaultConfig. Removing only the first leaves the native build fully wired.",
        "action": "neutralise",
        "neutralised_by": "if (liboPrebuiltEngine) {"
      },
      {
        "id": "assets-copyUnpackAssets",
        "file": "android/lib/build.gradle",
        "line_hint": 100,
        "anchor": "tasks.register('copyUnpackAssets', Copy)",
        "why": "Reads ${liboInstdir}, an engine build tree we never build. A Gradle Copy whose `from` is absent SUCCEEDS and copies nothing, so the APK ships the engine's .so files with its data half missing after a wholly green build. Must be disabled EXPLICITLY — relying on a missing directory to no-op is the defect, not the fix. NOTE its fonts.conf branch is a GENERATOR, not a copy; see assets.rebrand.",
        "action": "neutralise",
        "neutralised_by": "copyUnpackAssets.enabled = false"
      },
      {
        "id": "assets-copyAssets",
        "file": "android/lib/build.gradle",
        "line_hint": 172,
        "anchor": "tasks.register('copyAssets', Copy)",
        "why": "Same silent-empty-copy failure, for services.rdb, registry, filter, gallery and palette.",
        "action": "neutralise",
        "neutralised_by": "copyAssets.enabled = false"
      },
      {
        "id": "assets-createFullConfig",
        "file": "android/lib/build.gradle",
        "line_hint": 194,
        "anchor": "tasks.register('createFullConfig', Copy)",
        "why": "Same again for share/config, AND it is on preBuild.dependsOn, so it runs on every build of every variant.",
        "action": "neutralise",
        "neutralised_by": "createFullConfig.enabled = false"
      },
      {
        "id": "cool-bundle-release",
        "file": "android/lib/build.gradle",
        "line_hint": 335,
        "anchor": "tasks.register('generateCoolReleaseAssets')",
        "why": "KEPT, NOT NEUTRALISED. `afterEvaluate { generateReleaseAssets.dependsOn ... generateCoolReleaseAssets }` puts the npm/webpack `make -C ../../browser` on the RELEASE build graph, and that is the Node build this app needs: patches/0001 changes browser/, so the COOL bundle in the released APK is not ours. Recorded here so the anchor is still checked against upstream.",
        "action": "keep"
      }
    ],
    "cool_bundle_diagnostics": {
      "_doc": "THE KEPT ANCHOR ABOVE IS THE ONE THAT COULD FAIL WITHOUT SAYING WHY, AND ON 2026-09-16 IT DID. generateCoolReleaseAssets drives the COOL web-UI build by shelling out to `make -C ../../browser`, through Gradle's providers.exec. providers.exec CAPTURES the subprocess's stdout and stderr into the ExecOutput it returns and forwards neither to Gradle's logger, and upstream's two call sites ask only for `.result.get()` — so a non-zero exit arrives and the reason for it is discarded. Run 35038703895 is the measurement: 78 of 78 Android tasks executed, :lib:generateCoolReleaseAssets failed 37 seconds in, and all 1,270 lines of the job log held a Gradle stack trace, the string \"Process 'command 'make'' finished with non-zero exit value 2\", and NOT ONE LINE of make's own output. --info, which Gradle's own \"Try:\" block suggests, does not help: these streams never reach the logger at any verbosity. Every run of this workflow fetches a ~2 GB Gerrit tree, so a step that can only be diagnosed by guessing costs a fetch per guess. patches/0004 routes both generateCool*Assets tasks through one helper that sets ignoreExitValue, reads the two streams, prints them, and only then fails — and must NOT change what is built, which is what must_preserve pins.",
      "file": "android/lib/build.gradle",
      "patch": "0004",
      "upstream_discards_output": "}.result.get()",
      "_doc_upstream_discards_output": "The upstream form, asserted to STILL EXIST at upstream.online.revision. It is the defect: the ExecOutput is constructed, its result is demanded, and its standardOutput/standardError are never read. If upstream ever fixes this itself the tester reds and patches/0004 can be dropped rather than silently duplicating an upstream fix.",
      "must_add": [
        "ignoreExitValue",
        "standardOutput",
        "standardError",
        "logger.error"
      ],
      "_doc_must_add": "ignoreExitValue is what stops .result.get() throwing before the streams can be read; standardOutput and standardError are the streams; logger.error is what puts them in the job log. All four, because three of four is a task that captures the output and still does not print it.",
      "must_preserve": [
        "'clean'",
        "BUNDLE=DEBUG",
        "BUNDLE=RELEASE",
        "src/debug/assets/dist",
        "src/release/assets/dist"
      ],
      "_doc_must_preserve": "A DIAGNOSTICS PATCH THAT CHANGES WHAT IS BUILT IS NOT A DIAGNOSTICS PATCH. These are the make arguments and output directories of both variants; they must survive the rewrite verbatim, so the only difference patches/0004 makes is that a failure is legible.",
      "asserted_by": "tests/test-cool-make-failure-is-legible.sh"
    },
    "vendor_the_shell": {
      "_doc": "MEASURED, AND IT ANSWERS THE QUESTION IN THE PREFERRED DIRECTION. The fear behind _doc_no_source_in_git is LibreOffice core with external/, which is multi-gigabyte. It does not apply to the Android shell. A depth-1 clone of upstream.online at the pin was taken on 2026-09-11: .git is 480 MB (confirming the 471 MB figure in _doc_fetch_cost) and browser/ alone is 314 MB — but android/ is 2.5 MB across 259 files, and no file in it is remotely near the 100 MB blob cap that #162 exists to respect. Vendoring android/ therefore deletes a 480 MB, multi-minute Gerrit fetch from EVERY run and collapses patch_mode `apply` into an owned tree, which is the fleet's own 'dont fork, clone' pattern and what the other four fork apps already do. NOTE ON COST: that clone did NOT reproduce the ~10 minute figure from this container today — it sat in Gerrit's counting phase for over thirty minutes with no output before completing. Another reason not to put it on the critical path of every build.",
      "android_tree_bytes": 2621440,
      "android_tree_files": 259,
      "git_dir_bytes_at_pin": 503316480,
      "browser_dir_bytes": 329252864,
      "external_files_needed": [
        {
          "path": "coolkitconfig-mobile.xcu",
          "bytes": 28069,
          "why": "The ONLY file outside android/ the shell's gradle reads: android/lib/build.gradle's copyKitConfig takes ${rootProject.rootDir}/../coolkitconfig-mobile.xcu. Everything else it reaches for — templates/untitled.* — is android/templates/, inside the tree."
        }
      ],
      "configure_generated_files": {
        "_doc": "configure.ac:2922-2925 generates exactly four files into android/ via AC_CONFIG_FILES. Prebuilt mode does not need autotools to produce them: three are small substitutions we can emit from the values already in upstream.online.configure, and the fourth is deleted outright with the native build. THIS IS WHAT REMOVES THE configure.ac:884 --with-lo-builddir GATE ENTIRELY, rather than adding a --with-prebuilt-engine option to work around it — the gate is only reachable if we run configure, and prebuilt mode has no reason to.",
        "android/variables.gradle": "from android/variables.gradle.in; one value, @abs_top_builddir@",
        "android/app/appSettings.gradle": "from android/app/appSettings.gradle.in; @APP_NAME@ @VENDOR@ @INFO_URL@ @APP_PACKAGE_NAME@ are exactly upstream.online.configure, plus @COOLWSD_VERSION@ / @ANDROID_PACKAGE_VERSIONCODE@ / @CORE_VERSION_HASH@ (the last is engine.engine_revision.buildid, already read from the artefact)",
        "android/lib/libSettings.gradle": "from android/lib/libSettings.gradle.in; @LOBUILDDIR@ becomes the extracted payload root instead of an engine build tree",
        "android/lib/src/main/cpp/CMakeLists.txt": "from CMakeLists.txt.in — NOT generated at all in prebuilt mode, because the externalNativeBuild wiring that consumes it is removed"
      }
    },
    "_doc_action": "action/neutralised_by are what tests/test-prebuilt-mode-patch-covers-spec.sh reads: `neutralise` entries must be addressed by an ADDED line in patches/0002 carrying neutralised_by, and `keep` entries must not be. A specification nobody compares against the patch rots into fiction — the anchors tester already proves the anchors are real upstream; this proves the patch answers each one."
  },
  "patched_typescript": {
    "_doc": "THE SECOND TIME A PATCH WE WROTE WAS SHIPPED TO CI WITHOUT EVER HAVING BEEN COMPILED. test-patched-java-calls-kotlin-correctly.sh exists because run 35037615570 spent eight minutes discovering three lines of patches/0001 Java that could not compile. Run 35041370319 is the same shape one layer up: patches/0001's Settings menu entry calls window.COOLMessageHandler.postMobileMessage from Control.Menubar.ts, and the COOL release bundle build died on `tsc` with \"TS2339: Property 'COOLMessageHandler' does not exist on type 'Window & typeof globalThis'\" at Control.Menubar.ts:2535 and :2536. THE CALL WAS RIGHT AND THE TYPE WAS MISSING: LOActivity registers the bridge with addJavascriptInterface(this, \"COOLMessageHandler\") and its postMobileMessage(String) is @JavascriptInterface, so the object is real at runtime — but browser/ had only ever reached it from plain JavaScript, which upstream's build does not type-check (tsc runs --checkJs false), so ours was its first TypeScript caller and nothing in the tree declared it. NOTHING IN THIS REPOSITORY COULD HAVE CAUGHT IT: the patched TypeScript lives only inside a patch file, is compiled only after a ~2 GB Gerrit fetch, and both eslint and prettier pass on it (measured — prettier never even looks at Control.Menubar.ts, which is listed in browser/.beforeprettier). Hence the rule below, checked offline against the patch series plus the upstream files it edits.",
    "globals_object": "window",
    "declaration_keyword": "interface Window",
    "_doc_rule": "For every `window.<property>` our series ADDS to a .ts or .tsx file: either the POST-PATCH file declares <property> inside an `interface Window` block, or UPSTREAM's own copy of that same file already reads `window.<property>` — in which case upstream types it somewhere and tsc already accepts it. Anything else is a property that reaches TypeScript for the first time through us, undeclared, and stops the release bundle. Control.Menubar.ts is a global script rather than a module, so a bare `interface Window` merges into lib.dom's Window; that is why the declaration is written in the file that uses it instead of in a new one the build list would also have to learn about.",
    "asserted_by": "tests/test-patched-ts-declares-its-window-globals.sh"
  },
  "supply_chain": {
    "_doc": "WHAT REPLACES THE sha256 PIN, AND WHAT IS GENUINELY WEAKER. Stated here rather than in a README because the next person to touch this will read build.json.",
    "_doc_before": "BEFORE: one sha256 over Collabora's release APK. Strong and simple — the bytes either matched what Collabora published or the job failed. It also meant the app was signed by Collabora, and installing ours was byte-identical to installing from their F-Droid repo.",
    "_doc_after": "AFTER: ONE pinned git revision (online, full sha, verified at fetch time by tests/patches-apply.sh) plus ONE pinned binary artefact (upstream.engine, sha256 over Collabora's own release APK), plus a patch series that must apply exactly or the build stops. This is a change from the 2026-09-10 shape, which pinned TWO git revisions because it expected to compile the engine. It does not any more: the engine ships prebuilt, so for the 199 MB that is most of this app the ORIGINAL sha256-of-Collabora's-bytes guarantee is still in force. A commit sha is a hash over a whole tree's history, so neither pin is a weaker hash — they hash different things.",
    "_doc_weaker": "GENUINELY WEAKER, AND NARROWER THAN IT WAS. (1) We no longer verify a Collabora-SIGNED artefact; we verify its bytes by sha256 and then re-sign the repackaged result with the fleet key, so a compromise of that key now reaches Cloud Office where before it could not. (2) The layers we rebuild — the COOL web bundle and the Android shell — are built by us from pinned source, so for those the guarantee is source-and-toolchain rather than published-bytes. WHAT IS NO LONGER WEAK, and was on 2026-09-10: the engine. Taking it as a sha256-pinned prebuilt means no NDK toolchain, no external/ tarball tree and no engine build host enter the trust surface at all. The honest summary: we trade a narrow verifiable guarantee for ownership, over the ~20 percent of the APK we actually change.",
    "_doc_reproducible": "Reproducible in the sense that matters here: same two pins + same patch series + same configure flags + same toolchain = same source going into the compiler. Bit-identical APK output is NOT claimed and should not be until someone has actually diffed two builds."
  },
  "android": {
    "application_id": "com.diegonmarcos.cloudoffice",
    "_doc_application_id": "OURS NOW, AND IT CANNOT BE UPSTREAM'S. com.collabora.libreoffice belongs to bytes signed with Collabora's key; a rebuilt app signed with the fleet key cannot claim it, and Android would refuse the install anyway (signature mismatch on update). WHAT THE OWNER WILL EXPERIENCE: the new app does NOT replace the installed Collabora Office. It installs alongside it, as a second launcher icon, and the two coexist — different package names are different apps to Android in every respect. Documents opened by file association will offer both until the old one is removed. Nothing migrates automatically: the old app's settings and recent-files list stay with the old app. Removing the old one is a manual uninstall and is safe, because Collabora Office stores documents in normal storage rather than in its private data directory.",
    "min_sdk": 26,
    "abi_filters": [
      "arm64-v8a"
    ]
  },
  "signing": {
    "_doc": "ONE shared Cloud-constellation signing key for ALL Android apps. Cloud Office MUST be inside that set and this is not optional here: Text Enhance crosses an application boundary to the Cloud Keyboard over ITextTools, which is guarded by com.diegonmarcos.cloud.permission.CONSTELLATION_DATA at protectionLevel=signature. Signed with anything else, the platform refuses the bind and Text Enhance cannot work at all — it would fail with 'Text tools need the Cloud Keyboard app' on a phone where the keyboard is plainly installed. build.sh::_resolve_signing resolves the key from $VAULT_DIR + sops and FAILS LOUD if it cannot; there is no fallback and no generated key. See SIGNING.md.",
    "vault_keystore": "A0_keys/providers/android/release.jks",
    "vault_secrets": "A0_keys/providers/android/signing.secrets.yaml"
  },
  "build": {
    "_doc": "BUILT ON A STANDARD HOSTED RUNNER, BECAUSE NOTHING NATIVE IS COMPILED. The megabuild this key used to describe is not needed: patches/0002 packages both native libraries from the pinned released APK, so what runs is a Node build of browser/ and a Gradle build of android/. `mode` selects the generated workflow (1_cicd/src/templates/ship-<mode>.yml.in); `host` is its runs-on.",
    "mode": "source-prebuilt-engine",
    "host": "ubuntu-latest",
    "_doc_host": "SET 2026-09-15, together with patches/0002 and the generated source-build workflow — the three things this key said had to exist first. The toolchain half was already measured: com.android.tools.build:gradle:9.3.0, platforms;android-37.0 and build-tools;37.0.0 are all on stable channels a hosted runner can install; ndkVersion is declared upstream but never fetched, because no externalNativeBuild is wired and packaging keeps the released symbols instead of stripping them.",
    "sdk_packages": [
      "platforms;android-37.0",
      "platforms;android-36",
      "build-tools;37.0.0"
    ],
    "host_packages": [
      "autoconf",
      "automake",
      "libtool",
      "python3-lxml",
      "python3-polib"
    ],
    "gradle_task": "assembleRelease",
    "_doc_gradle_task": "assembleRelease, not a per-ABI task: android/app/build.gradle splits by ABI from liboAbiSplit and declares no product flavours, so the arm64-v8a APK is one output of assembleRelease.",
    "modules": {
      "text-tools": {
        "_doc": "Shared BY REFERENCE from ab_cloud-libs-shared/libs/text-tools — the ITextTools binder contract and its client, the same directory cloud-mail and the Cloud Keyboard build. materialize places it at android/text-tools inside the fetched online tree; patch 0001's settings.gradle include expects it there. A COPY MUST NEVER BE VENDORED: the AIDL's method order is a wire format, and two copies that disagree about it make an installed keyboard answer the wrong call.",
        "dir": "../ab_cloud-libs-shared/libs/text-tools",
        "materialize_to": "android/text-tools"
      }
    },
    "_doc_sdk_packages": "android-37.0 and build-tools 37.0.0 are what upstream's app and lib modules ask for (AGP 9's compileSdk { version = release(37) } resolves to the MINOR package platforms;android-37.0, not platforms;android-37). android-36 is for the shared text-tools module, which declares compileSdk 36 and is built by the AGP 8 apps that share it."
  },
  "text_enhance": {
    "_doc": "Cloud Office is a CALLER of the fleet enhancer, never a second copy of it. Routing, the model registry, the prompt chunking for oversized input, the timeout and the provider API KEY all stay in the Cloud Keyboard behind ITextTools; this app calls enhanceWith(), which exists precisely so a caller can hold its own settings and hand over its whole decision. cloud-mail was split from the keyboard the same way and for the same reason.",
    "_doc_settings_are_not_shared": "The owner's standing rule: a user-editable settings surface is an independently editable copy per app, never a shared link. What is shared is the network transport and the credential. Cloud Office's own choices live in its own SharedPreferences file ('cloud_text_enhance') with defaults from android/lib/src/main/assets/cloud_text_enhance.json, added by patch 0001.",
    "service_package": "com.diegonmarcos.cloudkeyboard",
    "permission": "com.diegonmarcos.cloud.permission.CONSTELLATION_DATA"
  },
  "tests": {
    "_doc": "WITHOUT THIS BLOCK NONE OF THIS APP'S TESTERS HAD EVER RUN, and nothing said so. cloud-android-test-engine.sh resolves .tests.shell.dir and DEFAULTS TO \"test\"; this app's directory is \"tests\", so the engine looked at ac_cloud-sheets/test, found nothing, printed one COVERAGE-GAP line and exited 0. The second half of the same defect is the glob: the engine runs only \"$dir\"/test-*.sh, and both testers here were named without that prefix, so even with the directory right they would have been skipped. Two files that call themselves 'the assertion the whole clone structure rests on' and the evidence behind the #241 verdict were decoration. Fixed by declaring the directory and by renaming the artefact tester to test-engine-artefacts-in-apk.sh.",
    "shell": {
      "dir": "tests",
      "_doc_requires": "Checked BEFORE any tester runs, because a tester that shells out to a missing tool does not fail — it answers from the tool's absence. test-engine-artefacts-in-apk.sh parses a ZIP central directory in python3 and range-fetches it with curl; test-configure-flags-exist.sh reads Gerrit's REST API with curl and decodes base64. test-sdkmanager-status-is-its-own.sh reads build.sdk_packages with jq and runs the engine's own sdkmanager pipeline under timeout, because the regression it guards against has a shape that HANGS rather than fails, and a suite that hangs is a suite that gets turned off. Absent any of these, those testers would report a verdict they never computed.",
      "requires": [
        "curl",
        "python3",
        "base64",
        "jq",
        "timeout"
      ],
      "_doc_not_a_tester": "patches-apply.sh IS DELIBERATELY NOT NAMED test-*.sh, and that is a decision rather than the accident above. It is the strongest assertion this app has — it fetches the pin and applies the series for real — but Gerrit refuses both blob filtering and sha fetches, so it measured 471 MB and about ten minutes from this container. That is the wrong shape for a per-push tester on a workflow that triggers on every commit under ac_cloud-office/**, and a ten-minute network step on every doc edit is how a suite starts getting ignored. It belongs to the source-build workflow as a release gate, run once against the pin, and to agents by hand. When build.host is set and that workflow exists, it gets an explicit step there — not this prefix."
    }
  },
  "release": {
    "kind": "app",
    "variants": [
      {
        "_doc": "arm64-v8a only, as the mirror was. Every additional ABI is another full NDK megabuild of LibreOffice core, and no phone in this fleet needs one.",
        "id": "arm64",
        "abis": [
          "arm64-v8a"
        ],
        "supported_abis": [
          "arm64-v8a"
        ],
        "artifact_debug": "Cloud-Office.apk",
        "gh_asset": "Cloud-Office.apk",
        "ghcr_tag_suffix": "",
        "update_tag": "latest"
      }
    ],
    "auto_update": {
      "enabled": true,
      "tag": "latest",
      "interval_hours": 6,
      "require_unmetered_network": true,
      "require_charging": false,
      "install_mode": "silent",
      "_doc_require_unmetered_network": "Still the largest artifact in the fleet by an order of magnitude. Unmetered-only so a background auto-update pass cannot burn a mobile data plan.",
      "max_installs_per_pass": 3
    },
    "ghcr": {
      "enabled": true,
      "registry": "ghcr.io",
      "namespace": "diegonmarcos",
      "image": "cloud-office",
      "media_type": "application/vnd.android.package-archive",
      "tags": [
        "latest",
        "sha-{sha}",
        "v{version_name}"
      ]
    },
    "gh_release": {
      "enabled": true,
      "rolling_tag": "latest",
      "asset_name": "Cloud-Office.apk",
      "draft": false,
      "prerelease": false,
      "generate_release_notes": false
    }
  },
  "published_artifact": {
    "_doc": "WHAT IS ACTUALLY ON THE RELEASE SHELF RIGHT NOW, AND IT IS NOT OURS. Every other key in this file describes the app we intend to build. NOTHING HAS EVER BUILT IT: build.host is null and ship-cloud-office.yml exits 1 before it publishes anything. So the only office artefact on the rolling `latest` release is still Cloud-Sheets.apk from the mirror era, and it is Collabora's own signed build republished under our release name. This block records that as DATA rather than leaving it as prose in a commit message, because the repository had already started reasoning as though the source build existed — test-sheets-mirror-pin.sh deleted its published-bytes checks on the stated premise 'we build com.diegonmarcos.cloudoffice, we are its only source', which is false and stays false until #240 is funded.",
    "_doc_verified": "READ OUT OF THE ARTEFACT ON 2026-09-10, not inferred from the filename. The ZIP central directory was range-fetched (6,375 entries), AndroidManifest.xml was decoded for its package attribute, META-INF/COLLABOR.RSA was decoded with openssl for the signing certificate, and all 267,216,449 bytes were streamed through sha256sum. All four agree with the row Collabora's own F-Droid index publishes for their arm64 APK, so these bytes are byte-identical to what their repo serves.",
    "built_by_this_fleet": false,
    "asset_name": "Cloud-Sheets.apk",
    "release_tag": "latest",
    "publisher": "Collabora Productivity",
    "package": "com.collabora.libreoffice",
    "version_name": "26.04.3.1",
    "version_code": 155,
    "abi": "arm64-v8a",
    "size": 267216449,
    "sha256": "b7ab381de96f429c2e762d0e54ee0fac7db03ad2fecfede9f4c558c767d18513",
    "signer_cert_sha256": "573258c84e149b5f4d9299e7434b2b69a8410372921d4ae586ba91ec767892cc",
    "signer_subject": "C=GB, L=Cambridge, O=Collabora Productivity Limited, OU=productivity, CN=Tomaz Vajngerl",
    "upstream_apk_name": "collabora-office-mobile-26.04.3.1-155-release-arm64-v8a-2026-09-03.apk",
    "fdroid_index": "https://www.collaboraoffice.com/downloads/fdroid/repo/index-v1.json",
    "_doc_do_not_rename": "DO NOT RENAME OR DELETE THIS ASSET AS A TIDY-UP. aa_cloud-superapp/build.json's phone-apps entry points install_apk_url at this exact filename, and it is the only office app the owner can install today. Renaming it hands him a 404; deleting it takes his office suite away to fix a labelling problem. The honest fix is to publish Cloud-Office.apk beside it, which needs a build host, not a rename.",
    "_doc_why_not_repackaged": "AND IT MUST NOT BE REPACKAGED. Injecting our dex into these bytes to make the Text Enhance menu item appear would break Collabora's signature, require re-signing with the fleet key, and put a modified binary of unclear provenance on the owner's phone while looking like a successful build. #241 answered the reuse question NO on the engine's own terms: 0 of 6,375 entries are .a or .h, and upstream needs eight static archives to link libandroidapp.so."
  },
  "_doc_source_identity": "source_identity.mode=\"pin\" IS DELIBERATELY GONE. It existed because a mirror's output was a pure function of `upstream`, so hashing the directory republished 250 MB whenever an unrelated repo-wide commit moved the tree sha — which happened on 2026-09-01. That reasoning DIES with the mirror: our output is now a function of the pins AND the patch series AND the configure flags, all of which live in this directory. Falling back to the default path hash is therefore correct, and leaving mode=pin would have been an active bug — every patch added here would have produced a byte-different APK that the gate believed was unchanged, and the phones would never have seen it."
}
