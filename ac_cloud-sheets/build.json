{
  "id": "ac_cloud-sheets",
  "name": "Cloud Office",
  "_doc": "SOURCE BUILD, as of this commit. This directory used to be a MIRROR: ship-cloud-sheets.yml downloaded Collabora's official APK, checked it against a pinned sha256 and republished those exact bytes. That gave a strong supply-chain guarantee and zero ownership — we could not rename the app, could not move its menus, and above all could not make Text Enhance work, because the feature needs code inside the web layer and the Android shell. The owner chose ownership. So we now build from source, and the sha256-of-upstream's-bytes guarantee is replaced by a pinned-revision guarantee; see supply_chain below, which states plainly what is weaker.",
  "_doc_no_source_in_git": "THE SOURCE IS NOT IN THIS REPOSITORY AND MUST NOT BE. LibreOffice core with external/ present is multi-gigabyte; this repo has a hard rule that no file may approach 100 MB, has already had to repair a 90 MB repository, and shards its transcripts to stay under GitHub's blob limit. Committing the tree would wreck it. Instead: two pinned upstreams fetched at build time, and every edit we own as a numbered patch in patches/. The fleet's other forks (dialer, matrix, vault, media-center) vendor their patched source instead — they moved to that on 2026-08-19 — and we deliberately cannot follow them, for size and nothing else. patches/README.md justifies the divergence in full.",
  "upstream": {
    "online": {
      "_doc": "Collabora Online — the web app in the WebView (browser/) AND the Android shell around it (android/). This is the ONLY tree we patch.",
      "vendor": "Collabora Productivity",
      "url": "https://gerrit.collaboraoffice.com/online",
      "_doc_url": "GERRIT, NOT GITHUB, AND THAT IS NOT A PREFERENCE. The GitHub mirror (CollaboraOnline/online) carries the Android tree only on distro/collabora/co-25.04-mobile; the 26.04 mobile line — the one matching the 26.04.3.1 APK this app shipped while it was a mirror — exists only on Collabora's own Gerrit. Pinning the GitHub mirror would have quietly moved the owner back a whole release line. github_mirror stays recorded as a read-only convenience for code search; it is never the fetch source.",
      "github_mirror": "https://github.com/CollaboraOnline/online",
      "ref": "distro/collabora/co-26.04-mobile",
      "revision": "794af0088b0369878461cc53de45129eaf08824a",
      "_doc_revision": "A COMMIT SHA, NOT A BRANCH. VERIFIED: patches/0001 applies cleanly to this exact revision with `git am --keep-non-patch` from a clean checkout, watched, not assumed. The SAME patch generated against 25.04-mobile FAILED here on three files — 'error: browser/src/map/Clipboard.js: patch does not apply' — which is precisely the silent drift a floating ref would have caused and the reason the sha is written down. tests/patches-apply.sh refuses anything that is not a full 40-character sha.",
      "patch_mode": "apply",
      "_doc_patch_mode": "apply = the series in patches/ is applied with `git am` at materialize time against `revision`. The fleet's other forks are `vendored` (patched source committed, patches kept only as history). Reading this field is how an engine tells the two apart without knowing which app it is looking at.",
      "license": "MPL-2.0",
      "configure": {
        "_doc": "THE REBRAND IS THESE FLAGS, NOT A PATCH. android/app/appSettings.gradle is generated from android/app/appSettings.gradle.in, whose @APP_NAME@ / @APP_PACKAGE_NAME@ / @VENDOR@ / @INFO_URL@ placeholders configure.ac substitutes (AC_CONFIG_FILES, configure.ac:2923), so upstream already exposes every branding value as a knob. Patching the generated file would hardcode into the tree what a flag sets, and the patch would then conflict on every upstream touch of that file.",
        "_doc_flag_names": "EVERY NAME HERE IS VERIFIED AGAINST configure.ac AT THE PIN, BECAUSE AUTOCONF DOES NOT CHECK THEM FOR US. This block said \"--with-android-package-name\" from 05b0c6c15 until 2026-09-11. No such option exists at this revision — upstream's is --with-app-package-name (AC_ARG_WITH at configure.ac:292), and there is no @ANDROID_PACKAGE_NAME@ placeholder anywhere; the applicationId comes from @APP_PACKAGE_NAME@. An unknown --with-* does NOT fail configure: autoconf prints 'unrecognized options' to stderr and continues, so $with_app_package_name would have stayed empty and APP_PACKAGE_NAME would have taken its default. VERIFIED BY READING configure.ac:1155-1169: the default is org.collabora.app for an Android build, and com.collabora.office.Mobile only when enable_iosapp=yes. So the megabuild would have spent its money producing an APK whose applicationId is UPSTREAM'S, while android.application_id, the fleet manifest, the GHCR image and the launcher's installed-app probe all name com.diegonmarcos.cloudoffice — an app that would not exist. tests/test-configure-flags-exist.sh now derives the required flag from upstream on every run (placeholder -> configure.ac variable -> flag) and fails closed, so this cannot regress silently and a future upstream rename reports its new name instead of passing.",
        "--with-app-name": "Cloud Office",
        "--with-app-package-name": "com.diegonmarcos.cloudoffice",
        "--with-vendor": "diegonmarcos.com",
        "--with-info-url": "https://linktree.diegonmarcos.com"
      },
      "gerrit_url": "https://gerrit.collaboraoffice.com/online",
      "_doc_fetch_cost": "MEASURED, so nobody plans CI around a guess: Gerrit refuses blob filtering (`warning: filtering not recognized by server, ignoring`) and will not serve an arbitrary sha (`upload-pack: not our ref`). A `git fetch --depth 1 origin distro/collabora/co-26.04-mobile` from this container produced a 471 MB .git and took roughly ten minutes. Any CI step fetching this needs that budget and a cache; it is not a thirty-second checkout."
    },
    "core": {
      "_doc": "LibreOffice core as Collabora ships it — the document ENGINE behind the WebView. We build it and we NEVER patch it. That is what keeps our series to nine files instead of a fork of an office suite, and it is why `patches/` stays reviewable by hand.",
      "url": "https://gerrit.collaboraoffice.com/core",
      "ref": "distro/collabora/co-26.04",
      "revision": "c1013fad0d38683630b48c01b6d5208b8d81181e",
      "_doc_revision": "Read from the ref's tip on 2026-09-10 via `git ls-remote`. Matched to the 26.04 online pin above: core and online must come from the same release line or the engine and the web layer disagree about the protocol between them. UNVERIFIED beyond existing — nothing here has built it, and no patch is applied to it, so the only claim made is that this sha was the tip of that ref at that reading.",
      "_doc_BLOCKER_this_whole_block_is_probably_wrong": "DO NOT FUND #240 WITHOUT SETTLING THIS FIRST. On the revision we pin, `online` IS A MONOREPO and the LibreOffice engine is its `engine/` subdirectory — verified 2026-09-11 by fetching engine/sw/Library_sw.mk and engine/vcl/source/gdi/virdev.cxx at online@794af008 (both HTTP 200), and the pinned online commit's own diff touches engine/vcl/source/gdi/virdev.cxx. Re-confirmed the same day that c1013fad is still the tip of refs/heads/distro/collabora/co-26.04 in the separate `core` repo, and a0_docs/eng-specs/cloud-office-prebuilt-engine-reuse.md §1 dates that commit 2026-04-01 — five months before the 2026-09-03 APK this line is supposed to match. So following this pin would build a five-month-stale engine against a September shell, which is exactly the silent drift the 25.04->26.04 re-pin already caught once. The eng-spec's recommendation 1 is to DELETE upstream.core and build engine/ out of the online pin. That is not done here because it changes how the build fetches its source and no build or build host exists yet to verify the new shape against — it is a design decision for whoever sets build.host, NOT a doc fix. Recorded as data so the next reader cannot miss it.",
      "license": "MPL-2.0"
    }
  },
  "supply_chain": {
    "_doc": "WHAT REPLACES THE sha256 PIN, AND WHAT IS GENUINELY WEAKER. Stated here rather than in a README because the next person to touch this will read build.json.",
    "_doc_before": "BEFORE: one sha256 over Collabora's release APK. Strong and simple — the bytes either matched what Collabora published or the job failed. It also meant the app was signed by Collabora, and installing ours was byte-identical to installing from their F-Droid repo.",
    "_doc_after": "AFTER: two pinned git revisions, both full commit shas, both verified at fetch time by tests/patches-apply.sh, plus a patch series that must apply exactly or the build stops. A commit sha is a hash over the whole tree's history, so it is not a weaker hash — it is a hash of a different thing.",
    "_doc_weaker": "GENUINELY WEAKER, THREE WAYS, AND NONE OF THEM IS RECOVERABLE BY TRYING HARDER. (1) We no longer verify a Collabora-signed artefact; we verify source and then trust our own build of it. (2) The build now has a large dependency surface — an NDK toolchain, the whole external/ tree LibreOffice downloads during its build, and a build host — none of which the sha256 pin ever had to trust, and external/'s own tarball hashes are checked by LibreOffice's build rather than by us. (3) The result is signed with the fleet key, so a compromise of that key now reaches Cloud Office too, where before it could not. The honest summary: we traded a narrow, verifiable guarantee for ownership, and the owner made that trade knowingly.",
    "_doc_reproducible": "Reproducible in the sense that matters here: same two pins + same patch series + same configure flags + same toolchain = same source going into the compiler. Bit-identical APK output is NOT claimed and should not be until someone has actually diffed two builds."
  },
  "android": {
    "application_id": "com.diegonmarcos.cloudoffice",
    "_doc_application_id": "OURS NOW, AND IT CANNOT BE UPSTREAM'S. com.collabora.libreoffice belongs to bytes signed with Collabora's key; a rebuilt app signed with the fleet key cannot claim it, and Android would refuse the install anyway (signature mismatch on update). WHAT THE OWNER WILL EXPERIENCE: the new app does NOT replace the installed Collabora Office. It installs alongside it, as a second launcher icon, and the two coexist — different package names are different apps to Android in every respect. Documents opened by file association will offer both until the old one is removed. Nothing migrates automatically: the old app's settings and recent-files list stay with the old app. Removing the old one is a manual uninstall and is safe, because Collabora Office stores documents in normal storage rather than in its private data directory.",
    "min_sdk": 26,
    "abi_filters": [
      "arm64-v8a"
    ]
  },
  "signing": {
    "_doc": "ONE shared Cloud-constellation signing key for ALL Android apps. Cloud Office MUST be inside that set and this is not optional here: Text Enhance crosses an application boundary to the Cloud Keyboard over ITextTools, which is guarded by com.diegonmarcos.cloud.permission.CONSTELLATION_DATA at protectionLevel=signature. Signed with anything else, the platform refuses the bind and Text Enhance cannot work at all — it would fail with 'Text tools need the Cloud Keyboard app' on a phone where the keyboard is plainly installed. build.sh::_resolve_signing resolves the key from $VAULT_DIR + sops and FAILS LOUD if it cannot; there is no fallback and no generated key. See SIGNING.md.",
    "vault_keystore": "A0_keys/providers/android/release.jks",
    "vault_secrets": "A0_keys/providers/android/signing.secrets.yaml"
  },
  "build": {
    "_doc": "WE DO NOT BUILD THIS ANYWHERE IN THIS REPO'S NORMAL CI. An NDK megabuild over LibreOffice core plus the whole external/ tree does not fit a standard GitHub-hosted runner in either time or disk. The host is a CONFIGURATION VALUE, deliberately, so that choosing it is a separate decision from this work — a sibling task is establishing which host can carry the compile, and nothing here bakes in an answer.",
    "host": null,
    "_doc_host": "null = no build host has been established yet, and the workflow must FAIL LOUDLY rather than fall back to a hosted runner that will die four hours in with a full disk. Set it to the runner label once the host exists; nothing else in this file changes.",
    "gradle_task": "assembleArm64-v8aRelease",
    "modules": {
      "text-tools": {
        "_doc": "Shared BY REFERENCE from ab_cloud-libs-shared/libs/text-tools — the ITextTools binder contract and its client, the same directory cloud-mail and the Cloud Keyboard build. materialize places it at android/text-tools inside the fetched online tree; patch 0001's settings.gradle include expects it there. A COPY MUST NEVER BE VENDORED: the AIDL's method order is a wire format, and two copies that disagree about it make an installed keyboard answer the wrong call.",
        "dir": "../ab_cloud-libs-shared/libs/text-tools",
        "materialize_to": "android/text-tools"
      }
    }
  },
  "text_enhance": {
    "_doc": "Cloud Office is a CALLER of the fleet enhancer, never a second copy of it. Routing, the model registry, the prompt chunking for oversized input, the timeout and the provider API KEY all stay in the Cloud Keyboard behind ITextTools; this app calls enhanceWith(), which exists precisely so a caller can hold its own settings and hand over its whole decision. cloud-mail was split from the keyboard the same way and for the same reason.",
    "_doc_settings_are_not_shared": "The owner's standing rule: a user-editable settings surface is an independently editable copy per app, never a shared link. What is shared is the network transport and the credential. Cloud Office's own choices live in its own SharedPreferences file ('cloud_text_enhance') with defaults from android/lib/src/main/assets/cloud_text_enhance.json, added by patch 0001.",
    "service_package": "com.diegonmarcos.cloudkeyboard",
    "permission": "com.diegonmarcos.cloud.permission.CONSTELLATION_DATA"
  },
  "tests": {
    "_doc": "WITHOUT THIS BLOCK NONE OF THIS APP'S TESTERS HAD EVER RUN, and nothing said so. cloud-android-test-engine.sh resolves .tests.shell.dir and DEFAULTS TO \"test\"; this app's directory is \"tests\", so the engine looked at ac_cloud-sheets/test, found nothing, printed one COVERAGE-GAP line and exited 0. The second half of the same defect is the glob: the engine runs only \"$dir\"/test-*.sh, and both testers here were named without that prefix, so even with the directory right they would have been skipped. Two files that call themselves 'the assertion the whole clone structure rests on' and the evidence behind the #241 verdict were decoration. Fixed by declaring the directory and by renaming the artefact tester to test-engine-artefacts-in-apk.sh.",
    "shell": {
      "dir": "tests",
      "_doc_requires": "Checked BEFORE any tester runs, because a tester that shells out to a missing tool does not fail — it answers from the tool's absence. test-engine-artefacts-in-apk.sh parses a ZIP central directory in python3 and range-fetches it with curl; test-configure-flags-exist.sh reads Gerrit's REST API with curl and decodes base64. Absent any of these, both would report a verdict they never computed.",
      "requires": [
        "curl",
        "python3",
        "base64"
      ],
      "_doc_not_a_tester": "patches-apply.sh IS DELIBERATELY NOT NAMED test-*.sh, and that is a decision rather than the accident above. It is the strongest assertion this app has — it fetches the pin and applies the series for real — but Gerrit refuses both blob filtering and sha fetches, so it measured 471 MB and about ten minutes from this container. That is the wrong shape for a per-push tester on a workflow that triggers on every commit under ac_cloud-sheets/**, and a ten-minute network step on every doc edit is how a suite starts getting ignored. It belongs to the source-build workflow as a release gate, run once against the pin, and to agents by hand. When build.host is set and that workflow exists, it gets an explicit step there — not this prefix."
    }
  },
  "release": {
    "kind": "app",
    "variants": [
      {
        "_doc": "arm64-v8a only, as the mirror was. Every additional ABI is another full NDK megabuild of LibreOffice core, and no phone in this fleet needs one.",
        "id": "arm64",
        "abis": [
          "arm64-v8a"
        ],
        "supported_abis": [
          "arm64-v8a"
        ],
        "artifact_debug": "Cloud-Office.apk",
        "gh_asset": "Cloud-Office.apk",
        "ghcr_tag_suffix": "",
        "update_tag": "latest"
      }
    ],
    "auto_update": {
      "enabled": true,
      "tag": "latest",
      "interval_hours": 6,
      "require_unmetered_network": true,
      "require_charging": false,
      "install_mode": "silent",
      "_doc_require_unmetered_network": "Still the largest artifact in the fleet by an order of magnitude. Unmetered-only so a background auto-update pass cannot burn a mobile data plan.",
      "max_installs_per_pass": 3
    },
    "ghcr": {
      "enabled": true,
      "registry": "ghcr.io",
      "namespace": "diegonmarcos",
      "image": "cloud-office",
      "media_type": "application/vnd.android.package-archive",
      "tags": [
        "latest",
        "sha-{sha}",
        "v{version_name}"
      ]
    },
    "gh_release": {
      "enabled": true,
      "rolling_tag": "latest",
      "asset_name": "Cloud-Office.apk",
      "draft": false,
      "prerelease": false,
      "generate_release_notes": false
    }
  },
  "published_artifact": {
    "_doc": "WHAT IS ACTUALLY ON THE RELEASE SHELF RIGHT NOW, AND IT IS NOT OURS. Every other key in this file describes the app we intend to build. NOTHING HAS EVER BUILT IT: build.host is null and ship-cloud-sheets.yml exits 1 before it publishes anything. So the only office artefact on the rolling `latest` release is still Cloud-Sheets.apk from the mirror era, and it is Collabora's own signed build republished under our release name. This block records that as DATA rather than leaving it as prose in a commit message, because the repository had already started reasoning as though the source build existed — test-sheets-mirror-pin.sh deleted its published-bytes checks on the stated premise 'we build com.diegonmarcos.cloudoffice, we are its only source', which is false and stays false until #240 is funded.",
    "_doc_verified": "READ OUT OF THE ARTEFACT ON 2026-09-10, not inferred from the filename. The ZIP central directory was range-fetched (6,375 entries), AndroidManifest.xml was decoded for its package attribute, META-INF/COLLABOR.RSA was decoded with openssl for the signing certificate, and all 267,216,449 bytes were streamed through sha256sum. All four agree with the row Collabora's own F-Droid index publishes for their arm64 APK, so these bytes are byte-identical to what their repo serves.",
    "built_by_this_fleet": false,
    "asset_name": "Cloud-Sheets.apk",
    "release_tag": "latest",
    "publisher": "Collabora Productivity",
    "package": "com.collabora.libreoffice",
    "version_name": "26.04.3.1",
    "version_code": 155,
    "abi": "arm64-v8a",
    "size": 267216449,
    "sha256": "b7ab381de96f429c2e762d0e54ee0fac7db03ad2fecfede9f4c558c767d18513",
    "signer_cert_sha256": "573258c84e149b5f4d9299e7434b2b69a8410372921d4ae586ba91ec767892cc",
    "signer_subject": "C=GB, L=Cambridge, O=Collabora Productivity Limited, OU=productivity, CN=Tomaz Vajngerl",
    "upstream_apk_name": "collabora-office-mobile-26.04.3.1-155-release-arm64-v8a-2026-09-03.apk",
    "fdroid_index": "https://www.collaboraoffice.com/downloads/fdroid/repo/index-v1.json",
    "_doc_do_not_rename": "DO NOT RENAME OR DELETE THIS ASSET AS A TIDY-UP. aa_cloud-superapp/build.json's phone-apps entry points install_apk_url at this exact filename, and it is the only office app the owner can install today. Renaming it hands him a 404; deleting it takes his office suite away to fix a labelling problem. The honest fix is to publish Cloud-Office.apk beside it, which needs a build host, not a rename.",
    "_doc_why_not_repackaged": "AND IT MUST NOT BE REPACKAGED. Injecting our dex into these bytes to make the Text Enhance menu item appear would break Collabora's signature, require re-signing with the fleet key, and put a modified binary of unclear provenance on the owner's phone while looking like a successful build. #241 answered the reuse question NO on the engine's own terms: 0 of 6,375 entries are .a or .h, and upstream needs eight static archives to link libandroidapp.so."
  },
  "_doc_source_identity": "source_identity.mode=\"pin\" IS DELIBERATELY GONE. It existed because a mirror's output was a pure function of `upstream`, so hashing the directory republished 250 MB whenever an unrelated repo-wide commit moved the tree sha — which happened on 2026-09-01. That reasoning DIES with the mirror: our output is now a function of the pins AND the patch series AND the configure flags, all of which live in this directory. Falling back to the default path hash is therefore correct, and leaving mode=pin would have been an active bug — every patch added here would have produced a byte-different APK that the gate believed was unchanged, and the phones would never have seen it."
}
